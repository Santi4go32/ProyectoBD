head
  meta(charset='utf-8')
  title Login

  link(rel='stylesheet', href='https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-alpha.2/css/bootstrap.min.css')
  link(rel='stylesheet', href='/stylesheets/style.css')

body
  .modal-dialog
        .modal-content
          // Modal Header
          .modal-header
            button.close(type="button", data-dismiss="modal")
              span(aria-hidden="true")
              span.sr-only Close
            h1#myModalLabel.modal-title
              | MANDE
            //Modal Body
            .modal-body
              form(role='form' method='POST')
                .form-group
                    input(type='text' name='usuarioId' required)
                    span(class='barra')
                    label(for='usuarioId') Celular(Usuario)/Cedula(Trabajador):  
                .form-group
                    input(type='password' name='contrasenha' required)
                    span(class='barra')
                    label(for='contrasenha') Contraseña:

                .form-group
                    input(type='text' name='trabajador' placeholder="Si/No")
                    label(for='trabajador' required) ¿Soy un trabajador?
                button.btn.btn-primary(type="submit", name="login") Iniciar Sesión